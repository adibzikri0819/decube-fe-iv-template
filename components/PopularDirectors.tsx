const PopularDirectors = ({ children }) => {
    return (
      <div className="flex flex-wrap justify-around items-center mt-4">
        {children}
      </div>
    );
  };
  
  export default PopularDirectors;